#!/usr/bin/env zsh

# === Global ===

alias code='code .'
alias open='open .'
alias composer='composer install'
alias re='clear; exec zsh -l'
alias flushdns='sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder'
